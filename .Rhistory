makeVector <- function(x = numeric()) {
m <- NULL
set <- function(y) {
x <<- y
m <<- NULL
}
get <- function() x
setmean <- function(mean) m <<- mean
getmean <- function() m
list(set = set, get = get,
setmean = setmean,
getmean = getmean)
}
makeVector <- function(x = numeric()) {
m <- NULL
set <- function(y) {
x <<- y
m <<- NULL
}
return(set)}
makeVector(5)
makeVector <- function(x = numeric()) {
m <- NULL
set <- function(y) {
x <<- y
m <<- NULL
}
get <- function() x
setmean <- function(mean) m <<- mean
getmean <- function() m
list(set = set, get = get,
setmean = setmean,
getmean = getmean)
}
makeVector <- function(x = numeric()) {
m <- NULL
set <- function(y) {
x <<- y
m <<- NULL
}
get <- function() x
setmean <- function(mean) m <<- mean
getmean <- function() m
list(set = set, get = get,
setmean = setmean,
getmean = getmean)
}
makeVector(5)
a <- makeVector(c(1:5))
a
a$getmean()
a$get()
a$set(c(5,6,7,8))
a$get()
a <- makeVector(c(2:9))
a$setmean(20)
a$getmean()
cachemean <- function(x, ...) {
m <- x$getmean()
if(!is.null(m)) {
message("getting cached data")
return(m)
}
data <- x$get()
m <- mean(data, ...)
x$setmean(m)
m
}
cachemean(a)
b <- c(30)
cachemean(b)
b <- c(20:30)
cachemean(b)
?solve
makeCacheMatrix <- function(x = matrix()) {
inv <- NULL
set <- function(y) {
x <<- y
inv <<- NULL
}
get <- function(x)
setsol <- function(sol) inv <<- sol
getsol <- function() sol
list(set = set, get = get, setsol = setsol, getsol = getsol)
}
c <- matrix(1:10, 2, 5)
c
makeCacheMatrix(c)
a
makeVector(b)
makeCacheMatrix(b)
makeCacheMatrix <- function(x = matrix()) {
inv <- NULL
set <- function(y) {
x <<- y
inv <<- NULL
}
get <- function(x)
setsol <- function(sol) inv <<- sol
getsol <- function() sol
list(set = set, get = get, setsol = setsol, getsol = getsol)
}
makeCacheMatrix(b)
makeCacheMatrix <- function(x = matrix()) {
inv <- NULL
set <- function(y) {
x <<- y
inv <<- NULL
}
get <- function(x)
setsol <- function(sol) {inv <<- sol}
getsol <- function() {sol}
list(set = set, get = get, setsol = setsol, getsol = getsol)
}
makeCacheMatrix(b)
makeCacheMatrix <- function(x = matrix()) {
inv <- NULL
set <- function(y) {
x <<- y
inv <<- NULL
}
get <- function(x)
## setsol <- function(sol) inv <<- sol
getsol <- function() sol
list(set = set, get = get, setsol = setsol, getsol = getsol)
}
makeCacheMatrix <- function(x = matrix()) {
inv <- NULL
set <- function(y) {
x <<- y
inv <<- NULL
}
get <- function(x)
setsol <- function(solve) inv <<- solve
getsol <- function() solve
list(set = set, get = get, setsol = setsol, getsol = getsol)
}
makeCacheMatrix(b)
makeVectortest <- function(x = matrix()) {
m <- NULL
set <- function(y) {
x <<- y
m <<- NULL
}
get <- function() x
setmean <- function(mean) m <<- mean
getmean <- function() m
list(set = set, get = get,
setmean = setmean,
getmean = getmean)
}
makeVectortest(c)
makeVectortest <- function(x = matrix()) {
inv <- NULL
set <- function(y) {
x <<- y
inv <<- NULL
}
get <- function() x
setmean <- function(mean) inv <<- mean
getmean <- function() m
list(set = set, get = get,
setmean = setmean,
getmean = getmean)
}
makeVectortest(c)
makeVectortest <- function(x = matrix()) {
inv <- NULL
set <- function(y) {
x <<- y
inv <<- NULL
}
get <- function() x
setsolv <- function(solve) inv <<- solve
getmean <- function() inv
list(set = set, get = get,
setsolv = setsolv,
getmean = getmean)
}
makeVectortest(c)
makeVectortest <- function(x = matrix()) {
inv <- NULL
set <- function(y) {
x <<- y
inv <<- NULL
}
get <- function() x
setsolv <- function(solve) inv <<- solve
getsolv <- function() inv
list(set = set, get = get,
setsolv = setsolv,
getsolv = getsolv)
}
makeVectortest(c)
makeVectortest <- function(x = matrix()) {
inv <- NULL
set <- function(y) {
x <<- y
inv <<- NULL
}
get <- function() x
setsol <- function(solve) inv <<- solve
getsol <- function() inv
list(set = set, get = get,
setsol = setsol,
getsol = getsol)
}
makeVectortest(c)
makeCacheMatrixTest <- function(x = matrix()) {
inv <- NULL
set <- function(y) {
x <<- y
inv <<- NULL
}
get <- function() x
setsol <- function(solve) inv <<- solve
getsol <- function() inv
list(set = set, get = get,
setsol = setsol,
getsol = getsol)
}
makeVectortest(c)
makeCacheMatrix <- function(x = matrix()) {
inv <- NULL
set <- function(y) {
x <<- y
inv <<- NULL
}
get <- function() x
setsol <- function(solve) inv <<- solve
getsol <- function() solve
list(set = set, get = get, setsol = setsol, getsol = getsol)
}
makeCacheMatrix(c)
d <- makeCacheMatrix(c)
d$get()
makeCacheMatrix(10:20, 2, 5)
c2 <- matrix(10:20, 2, 5)
c2
c2 <- matrix(11:20, 2, 5)
c2
d$setsol(c2)
d$getsol()
d$get()
d$setsol(10)
d$getsol()
d$get()
makeCacheMatrix <- function(x = matrix()) {
inv <- NULL
set <- function(y) {
x <<- y
inv <<- NULL
}
get <- function() x
setsol <- function(solve) inv <<- solve
getsol <- function() inv
list(set = set, get = get, setsol = setsol, getsol = getsol)
}
d
d$get()
d$setsol(c2)
d$getsol()
makeCacheMatrix <- function(x = matrix()) {
inv <- NULL
set <- function(y) {
x <<- y
inv <<- NULL
}
get <- function() x
setsol <- function(solve) inv <<- solve
getsol <- function() inv
list(set = set, get = get, setsol = setsol, getsol = getsol)
}
d <- makeCacheMatrix(c)
d$setsol(c2)
d$get()
d$getsol
e <- makeCacheMatrix(c2)
e
e$get()
e$setsol(c)
e$getsol
ls
ls()
rm(list = ls)
rm(list = ls())
makeCacheMatrix <- function(x = matrix()) {
inv <- NULL
set <- function(y) {
x <<- y
inv <<- NULL
}
get <- function() x
setsol <- function(solve) inv <<- solve
getsol <- function() inv
list(set = set, get = get, setsol = setsol, getsol = getsol)
}
matrix1 <- matrix(1:10, 2, 5)
matrix2 <- matrix(11:20, 2, 5)
cm1 <- makeCacheMatrix(matrix1)
cm1$get()
cm1$set(matrix2)
cm1$get()
cm1$set(matrix1)
cm1$get()
cm1$setsol(matrix2)
cm1$getsol()
cacheSolve <- function(x, ...) {
inv <- x$getsol()
if(!is.null(inv)) {
message("getting cached data")
return(inv)
}
}
cacheSolve(cm1)
cacheSolve <- function(x, ...) {
inv <- x$getsol()
if(!is.null(inv)) {
message("getting cached data")
return(inv)
}
data <- x$get()
inv <- solve(data, ...)
x$setsol(inv)
return(inv)
}
cm1$set(NULL)
cm1$get()
cm1$getsol()
cm1 <- makeCacheMatrix(matrix1)
cm1$get()
cm1$getsol
cm1$getsol()
cacheSolve(cm1)
solve(matrix1)
?matrix
matrix1 <- matrix(1:9, 3, 3)
matrix1
solve(matrix1)
randommatrix<-function(x) {
matrix(x, 3, 3)
]
randommatrix<-function(x) {
matrix(x, 3, 3)          }
randommatrix(rnorm(9))
matrix3 <- randommatrix(9)
solve(matrix3)
